# CATEGORY
epita_adm:
  # NAME
  name: EPITA/ADM
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    administration:
      # NAME
      name: administration
      #PERM
      overwrites: ~
      default_perm: DEFAULT


    staff_epilogin_only:
      # displayed name
      name: test-staff-epilogin

      #perm overwrites for some roles
      overwrites:
        EPITA: DEFAULT
        ADMIN: ADMIN_PERM
        STAFF_EPILOGIN: ADMIN_PERM

      default_perm: DEFAULT

  # VOCAL CHANNELS
  vocal_channels: ~

