# CATEGORY
epita_adm:
  # NAME
  name: EPITA/ADM👥
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    administration:
      # NAME
      name: administration
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: HIDDEN

    moderation:
      # NAME
      name: moderation
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
      default_perm: HIDDEN

    adm_room:
      # NAME
      name: Adm_Epita
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
        ADM: MANAGE_MESSAGE
        PROF: MANAGE_MESSAGE
      default_perm: HIDDEN

    acdc_room:
      # NAME
      name: ACDC_room
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        ACDC: MANAGE_MESSAGE
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels:

    reunion:
      # NAME
      name: Réunion
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: VOCAL
        ADM: VOCAL
        PROF: VOCAL
      default_perm: HIDDEN

