# CATEGORY
epita_adm:
  # NAME
  name: EPITA/ADM👥
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    administration:
      # NAME
      name: administration
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: HIDDEN

    moderation:
      # NAME
      name: moderation
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
      default_perm: HIDDEN

    adm_room:
      # NAME
      name: adm_Epita
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
        ADM: MANAGE_MESSAGE
        PROF: MANAGE_MESSAGE
      default_perm: HIDDEN

    acdc_room:
      # NAME
      name: acdc_room
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        ACDC: MANAGE_MESSAGE
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels:

    reunion:
      # NAME
      name: Réunion
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: VOCAL
        ADM: VOCAL
        PROF: VOCAL
      default_perm: HIDDEN

epita_bot:
  # NAME
  name: EPITA/BOT🤖
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    presentation:
      # NAME
      name: presentation
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_ONLY
        EXTERNE: HIDDEN
        EPITA: HIDDEN
      default_perm: READ_AND_WRITE_WITHOUT_HISTORY

    epilogin_logs:
      # NAME
      name: epilogin_logs
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_ONLY
      default_perm: HIDDEN

    epilogin_admin:
      # NAME
      name: epilogin_admin
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_ONLY
      default_perm: HIDDEN

    epimodo_logs:
      # NAME
      name: epimodo_logs
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_ONLY
      default_perm: HIDDEN

    epimodo_requests:
      # NAME
      name: epimodo_requests
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita:
  # NAME
  name: EPITA/ 👍
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: DEFAULT
  # CHANNELS
  channels:

    annonces:
      # NAME
      name: annonces
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_AND_WRITE
        ACDC: READ_AND_WRITE
        EPITA: READ_ONLY
      default_perm: READ_ONLY

    info_exam:
      # NAME
      name: info_exam
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_AND_WRITE
        ACDC: READ_AND_WRITE
        EPITA: READ_ONLY
      default_perm: HIDDEN

    associations:
      # NAME
      name: associations
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_AND_WRITE
        ASSO: READ_AND_WRITE
        EPITA: READ_ONLY
      default_perm: READ_ONLY

    vie_etudiante:
      # NAME
      name: vie_etudiante
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: READ_AND_WRITE
        EPITA: READ_ONLY
      default_perm: READ_ONLY

    acdc_eleves:
      # NAME
      name: acdc-eleves
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        ACDC: MANAGE_MESSAGE
        PROMO_N: READ_ONLY
        PROMO_N_MINUS_1: READ_ONLY
        PROMO_N_MINUS_2: READ_ONLY
        PROMO_N_MINUS_3: READ_ONLY
      default_perm: HIDDEN


    liens_utiles:
      # NAME
      name: liens_utiles
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: READ_ONLY

    objets_trouves:
      # NAME
      name: objets_trouves
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        ACDC: MANAGE_MESSAGE
      default_perm: READ_ONLY

  # VOCAL CHANNELS
  vocal_channels: ~

epita_global:
  # NAME
  name: EPITA/GLOBAL/ 🌐
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: DEFAULT
  # CHANNELS
  channels:

    general:
      # NAME
      name: general
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: READ_AND_WRITE

    qcm_exams:
      # NAME
      name: qcm_exams
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        EPITA: READ_AND_WRITE
      default_perm: HIDDEN

    profs_eleves:
      # NAME
      name: profs-eleves
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        EPITA: READ_AND_WRITE
      default_perm: HIDDEN

    cgu_faq:
      # NAME
      name: cgu_faq
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        EPITA: READ_AND_WRITE
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_promo:
  # NAME
  name: EPITA/PROMO
  # PERM
  overwrites:
    PROMO_N: READ_AND_WRITE
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    promo:
      # NAME
      name: PROMO
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: HIDDEN

    presentation:
      # NAME
      name: presentation
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
      default_perm: HIDDEN

    delegues:
      # NAME
      name: delegues
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        DELEGUE: READ_AND_WRITE
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_matieres:
  # NAME
  name: EPITA/MATIERES/ 📚
  # PERM
  overwrites:
    STAFF_EPILOGIN: SERVER_ADMIN_PERM
    ADMIN: ADMIN_PERM
    MODO: MODO_PERM
    ADM: HIDDEN
    PROF: MANAGE_MESSAGE
    PROMO_N: READ_AND_WRITE
    PROMO_N_MINUS_1: READ_AND_WRITE
    PROMO_N_MINUS_2: READ_AND_WRITE
    PROMO_N_MINUS_3: READ_AND_WRITE
    PROMO_N_MINUS_4: READ_AND_WRITE
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    corrections:
      # NAME
      name: corrections
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
        ADM: HIDDEN
        PROF: MANAGE_MESSAGE
        PROMO_N: READ_ONLY
        PROMO_N_MINUS_1: READ_ONLY
        PROMO_N_MINUS_2: READ_ONLY
        PROMO_N_MINUS_3: READ_ONLY
        PROMO_N_MINUS_4: READ_ONLY
      default_perm: HIDDEN

    archi:
      # NAME
      name: archi
      #PERM
      overwrites: ~
      default_perm: HIDDEN

    algo:
      # NAME
      name: algo
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    prog:
      # NAME
      name: prog
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    maths:
      # NAME
      name: maths
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    physique:
      # NAME
      name: physique
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    elec:
      # NAME
      name: elec
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    anglais:
      # NAME
      name: anglais
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    te:
      # NAME
      name: te
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    afit:
      # NAME
      name: afit
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    aide:
      # NAME
      name: aide
      #PERM
      overwrites: ~
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_projet:
  # NAME
  name: EPITA/PROJET/ 💻
  # PERM
  overwrites:
    STAFF_EPILOGIN: SERVER_ADMIN_PERM
    ADMIN: ADMIN_PERM
    MODO: MODO_PERM
    ADM: HIDDEN
    PROF: MANAGE_MESSAGE
    PROMO_N: READ_AND_WRITE
    PROMO_N_MINUS_1: READ_AND_WRITE
    PROMO_N_MINUS_2: READ_AND_WRITE
    PROMO_N_MINUS_3: READ_AND_WRITE
    PROMO_N_MINUS_4: READ_AND_WRITE
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    corrections:
      # NAME
      name: exemple
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
        ADM: HIDDEN
        PROF: MANAGE_MESSAGE
        PROMO_N: READ_ONLY
        PROMO_N_MINUS_1: READ_AND_WRITE
        PROMO_N_MINUS_2: READ_AND_WRITE
        PROMO_N_MINUS_3: READ_AND_WRITE
        PROMO_N_MINUS_4: READ_AND_WRITE
      default_perm: HIDDEN

    projet_s2:
      # NAME
      name: projet_s2
      #PERM
      overwrites: ~
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_groups:
  # NAME
  name: EPITA/GROUPES 👥
  # PERM
  overwrites:
    STAFF_EPILOGIN: SERVER_ADMIN_PERM
    ADMIN: ADMIN_PERM
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    A1:
      # NAME
      name: a1
      #PERM
      overwrites:
        A1: READ_AND_WRITE
      default_perm: HIDDEN
    A2:
      # NAME
      name: a2
      #PERM
      overwrites:
        A2: READ_AND_WRITE
      default_perm: HIDDEN
    B1:
      # NAME
      name: b1
      #PERM
      overwrites:
        B1: READ_AND_WRITE
      default_perm: HIDDEN
    B2:
      # NAME
      name: b2
      #PERM
      overwrites:
        B2: READ_AND_WRITE
      default_perm: HIDDEN
    C1:
      # NAME
      name: c1
      #PERM
      overwrites:
        C1: READ_AND_WRITE
      default_perm: HIDDEN
    C2:
      # NAME
      name: c2
      #PERM
      overwrites:
        C2: READ_AND_WRITE
      default_perm: HIDDEN
    D1:
      # NAME
      name: d1
      #PERM
      overwrites:
        D1: READ_AND_WRITE
      default_perm: HIDDEN
    D2:
      # NAME
      name: d2
      #PERM
      overwrites:
        D2: READ_AND_WRITE
      default_perm: HIDDEN
    INT1:
      # NAME
      name: int1
      #PERM
      overwrites:
        INT1: READ_AND_WRITE
      default_perm: HIDDEN
    INT2:
      # NAME
      name: int2
      #PERM
      overwrites:
        INT2: READ_AND_WRITE
      default_perm: HIDDEN
    INT3:
      # NAME
      name: int3
      #PERM
      overwrites:
        INT3: READ_AND_WRITE
      default_perm: HIDDEN
    SHARP:
      # NAME
      name: sharp
      #PERM
      overwrites:
        SHARP: READ_AND_WRITE
      default_perm: HIDDEN
    LYON:
      # NAME
      name: lyon
      #PERM
      overwrites:
        LYON: READ_AND_WRITE
      default_perm: HIDDEN
    TOULOUSE:
      # NAME
      name: toulouse
      #PERM
      overwrites:
        TOULOUSE: READ_AND_WRITE
      default_perm: HIDDEN
    RENNES:
      # NAME
      name: rennes
      #PERM
      overwrites:
        RENNES: READ_AND_WRITE
      default_perm: HIDDEN
    STRASBOURG:
      # NAME
      name: strasbourg
      #PERM
      overwrites:
        STRASBOURG: READ_AND_WRITE
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_divers:
  # NAME
  name: EPITA/DIVERS/ 🎮
  # PERM
  overwrites:
    STAFF_EPILOGIN: SERVER_ADMIN_PERM
    ADMIN: ADMIN_PERM
    MODO: MODO_PERM
    ADM: HIDDEN
    PROF: HIDDEN
    PROMO_N: READ_AND_WRITE
    PROMO_N_MINUS_1: READ_AND_WRITE
    PROMO_N_MINUS_2: READ_AND_WRITE
    PROMO_N_MINUS_3: READ_AND_WRITE
    PROMO_N_MINUS_4: READ_AND_WRITE
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    memes:
      # NAME
      name: memes
      #PERM
      overwrites: ~
      default_perm: HIDDEN

    gaming:
      # NAME
      name: gaming
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    dev:
      # NAME
      name: dev
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    films_tv:
      # NAME
      name: films_tv
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    musique:
      # NAME
      name: musique
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    video:
      # NAME
      name: video
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    stage:
      # NAME
      name: stage
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    logements:
      # NAME
      name: logements
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    enigmes:
      # NAME
      name: enigmes
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    spam:
      # NAME
      name: spam
      #PERM
      overwrites: ~
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels: ~

epita_bunker:
  # NAME
  name: EPITA/Bunker 🏛
  # PERM
  overwrites: ~
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    zone_de_quarantaine:
      # NAME
      name: zone_de_quarantaine
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: MODO_PERM
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels:

    radio_quarantaine:
      # NAME
      name: radio_quarantaine
      #PERM
      overwrites:
        STAFF_EPILOGIN: SERVER_ADMIN_PERM
        ADMIN: ADMIN_PERM
        MODO: VOCAL
      default_perm: HIDDEN

vocal:
  # NAME
  name: VOCAL
  # PERM
  overwrites:
    STAFF_EPILOGIN: SERVER_ADMIN_PERM
    ADMIN: ADMIN_PERM
    EPITA: READ_AND_WRITE_AND_VOCAL
    EXTERNE: READ_AND_WRITE_AND_VOCAL
  # EVERYONE
  default_perm: HIDDEN
  # CHANNELS
  channels:

    no-mic:
      # NAME
      name: mic
      #PERM
      overwrites: ~
      default_perm: HIDDEN

  # VOCAL CHANNELS
  vocal_channels:

    general:
      # NAME
      name: General
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    travail:
      # NAME
      name: Travail
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    gaming:
      # NAME
      name: Gaming
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    musique:
      # NAME
      name: Musique
      #PERM
      overwrites: ~
      default_perm: HIDDEN
    afk:
      # NAME
      name: AFK
      #PERM
      overwrites: ~
      default_perm: HIDDEN